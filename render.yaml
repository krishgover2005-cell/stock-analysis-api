services:
  - type: web
    name: stock-analysis-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn stock_api_server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
